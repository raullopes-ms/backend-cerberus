node_modules

# Keep environment variables out of version control
# `.env.example` é a única versão versionada.
.env
.env.*
!.env.example

# Artefatos gerados: saem do `npm run build` e do `prisma generate`.
/build
/generated/prisma
